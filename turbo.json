{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "@dupply/diagnostico#build": {
      "dependsOn": ["@dupply/types#build"],
      "outputs": ["dist/**"]
    },
    "@dupply/types#build": {
      "outputs": ["dist/**"]
    },
    "@dupply/console#build": {
      "dependsOn": ["@dupply/web#build", "^build"],
      "outputs": ["dist/**", "../web/dist/console/**"]
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
